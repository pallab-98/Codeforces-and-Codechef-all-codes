#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    while (1)
    {
        int num;
        cin >> num;
        if (num == 42)
            break;
        else
            cout << num << endl;
    }

    return 0;
}

