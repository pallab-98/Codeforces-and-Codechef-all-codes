#include <stdio.h>
int main()
{
    int index;
    scanf("%d", &index);
    arra[n][n];
    for (int i = 0; i < n; i++)
    {
        printf("\n");
        for (int j = 0; j < n; j++)
        {
            arra[i][j] = (1 + i) * (1 + j);
            printf("\t %d x %d = %d", 1 + i, 1 + j, arra[i][j]);
        }
    }
    return 0;
}