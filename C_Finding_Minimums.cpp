#include<bits/stdc++.h>
using namespace std;
int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    long long index,parts;
    cin>>index>>parts;
    long long ara[index];
    int con=0;
    while(con<index){
        cin>>ara[con];
        con++;
    }
    int value=index/parts;
    if(index%parts!=0)
    value++;

    int ara2[value];
    int con1=0;
    while(con1<=value){
        
    } 
cout<<value<<endl;

}