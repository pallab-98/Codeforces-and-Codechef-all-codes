#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int day, sunday;
    cin >> day >> sunday;
    cout << day * 6 + sunday << endl;

    return 0;
}
