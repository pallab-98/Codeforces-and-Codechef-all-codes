#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    
    int caseTime;
    cin >> caseTime;
    while (caseTime--)
    {
        long long a;
        cin >> a;
        cout << (a + 1) / 2 << endl;
    }

    return 0;
}