#include<bits/stdc++.h>
using namespace std;
int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    vector<cahr>s.(100);
    cin>>s;
    length.s();
    int spcecont=0;
    for(char cha: s)
    if(char cha==' '){
        spcecont++;
    }
}