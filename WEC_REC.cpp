#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    int caseTest;
    cin>>caseTest;
    while (caseTest--)
    {
        int n,x,k;
        cin>>n>>x>>k;
        int ara[n];
        for(int i=0;i<n;i++){
            cin>>ara[i];
        }
        sort(ara.begin(),ara.end(),greater<int>());
        for(int i=0;i<n;i++){
            ara[i]
        }

    }
    

    return 0;
}