#include <bits/stdc++.h>
using namespace std;

int main() 
{ 
    ios::sync_with_stdio(false); 
    cin.tie(nullptr);
    cout.tie(nullptr);
    
    int caseTest,count=0;
    cin >> caseTest;
    while (caseTest--)
    {   count++;
        cout<<count<<endl;
        
    } 
    return 0; 
}