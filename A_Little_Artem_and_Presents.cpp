#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cout.tie(nullptr);
    cin.tie(nullptr);

    long long n;
    cin >> n;

    cout << (2 * n + 1) / 3 << endl;

    return 0;
}