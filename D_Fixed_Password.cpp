#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    while (1)
    {

        int pass;
        cin >> pass;
        
        if (pass == 1999)
        {
            cout << "Correct\n";
            break;
        }
        else
            cout << "Wrong\n";
    }
    return 0;
}