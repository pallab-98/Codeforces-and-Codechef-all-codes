#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    string s1, s2;
    cin >> s1;
    cin >> s2;
    int len1, len2;
    len1 = s1.size(), len2 = s2.length();

    cout << len1 << " " << len2 << endl;
    cout << s1 << " " << s2 << endl;

    return 0;
}