#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int length, weight;
    cin >> length >> weight;
    cout << length * weight << endl;
    return 0;
}